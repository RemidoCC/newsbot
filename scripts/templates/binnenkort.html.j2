{% extends "base.html.j2" %}
{% block title %}newsbot — {{ titel }}{% endblock %}

{% block content %}
<div class="wrap">
  <div class="datumkop">
    <h1>{{ titel }}</h1>
    <p class="datumkop-sub">{{ toelichting }}</p>
  </div>
  <p class="leeg">Deze pagina wordt in {{ fase }} gebouwd.</p>
</div>
{% endblock %}
